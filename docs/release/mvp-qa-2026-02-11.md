# MVP QA Report - 2026-02-11

## Environment
- Date: 2026-02-11
- Repo: `catsafe-plants`
- Local Node in shell: `v18.14.0` (not compatible with Next.js 16 build command via `npm run build`)
- Verification runtime used for checks: `npx node@20` (`v20.20.0`)
- CI workflow files in repo: none found under `.github/workflows`

## Automated checks

### 1) Tests
- Command: `npx -y node@20 node_modules/vitest/vitest.mjs run --reporter=dot`
- Result: PASS
- Evidence: `5` test files passed, `36` tests passed

### 2) Lint
- Command: `npx -y node@20 node_modules/eslint/bin/eslint.js .`
- Result: PASS
- Evidence: exited `0` with no lint errors

### 3) Production build
- Command: `npx -y node@20 node_modules/next/dist/bin/next build`
- Result: PASS
- Evidence: Next.js `16.1.6` production build completed; app routes generated (`/`, `/plants`, `/plants/[id]`)

### 4) Citation audit
- Command: `npx -y node@20 scripts/audit-citations.mjs`
- Result: PASS
- Evidence: `Citation audit passed for 50 plants and 50 unique citation URLs.`

## Manual QA checklist (UI/flows)
- Happy path (home search -> detail -> alternatives -> directory): NOT RUN in this CLI session
- Empty state checks: NOT RUN manually in this CLI session
- Error state checks: NOT RUN manually in this CLI session
- Loading state checks: NOT RUN manually in this CLI session
- Mobile viewport sanity check: NOT RUN manually in this CLI session
- Keyboard/focus sanity check: NOT RUN manually in this CLI session

## Known issues / follow-ups
- Local default runtime (`v18.14.0`) is below Next.js 16 minimum; developers need Node 20+ when running build/release checks.
- No CI workflow currently enforces the Node runtime; if CI is added, pin Node `>=20.9.0`.
- Manual UI QA checklist remains pending in a browser session.

## Ship recommendation
- **Conditional GO** for technical release gate: automated checks passed under Node 20 baseline.
- **Before final publish:** complete manual browser QA checklist and record outcomes.

## Netlify Context Verification Checklist (Added 2026-02-16)
Use this checklist for every Netlify release candidate:

1. Confirm Netlify **Production** context variables:
   - `NEXT_PUBLIC_SUPABASE_URL` points to production Supabase.
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` points to production Supabase.
2. Confirm Netlify **Deploy Preview** context variables:
   - `NEXT_PUBLIC_SUPABASE_URL` points to preview Supabase.
   - `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` points to preview Supabase.
3. Trigger a Deploy Preview and validate:
   - Home search, detail, and directory load expected preview data.
   - Manual browser spot-check passes on key flows.
4. Run release gates before production publish:
   - `npm run audit:citations`
   - `npm run test:run`
   - `npm run lint`
   - `npm run build`
5. After production deploy, run:
   - Manual browser spot-check on production key flows.
